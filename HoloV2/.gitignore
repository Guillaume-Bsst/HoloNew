# Regenerable build-once cache (SDF grids, per-subject/geometry correspondence, calibration).
# Managed by prepare/. Keep only the committed neutral-correspondence default.
cache/**
!cache/correspondence/
!cache/correspondence/corr_neutral.npz

__pycache__/
*.pyc

# Machine-local path registry (copy from paths.example.toml). Keep the example tracked.
paths.toml
