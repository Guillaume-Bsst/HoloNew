"""2D panels (diagnostics non-3D sur la séquence entière) — distinct des ``layers/`` 3D.
Chaque panel lit ``solved.*`` sur tous les frames baked (fournis par la Source) et rend
dans la GUI viser. viser/matplotlib sont confinés ici (consommateur, règle d'or 6)."""
